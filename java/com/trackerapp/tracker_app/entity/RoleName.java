package com.trackerapp.tracker_app.entity;

/**
 * Defines the set of roles available in the application.
 * USER: standard application user with access to their own trackers.
 * ADMIN: elevated account with permissions to manage other users.
 */

public enum RoleName {

    ROLE_USER,
    ROLE_ADMIN
}
